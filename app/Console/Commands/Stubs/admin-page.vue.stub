<script setup lang="ts">
import SchemaRenderer from '@/components/Admin/Schema/SchemaRenderer.vue';
import AdminLayout from '@/layouts/AdminLayout.vue';
import { Head } from '@inertiajs/vue3';

defineOptions({ layout: AdminLayout });

defineProps<{
    schema: Array<Record<string, unknown>>;
    title?: string;
    initialData?: Record<string, unknown>;
}>();
</script>

<template>
    <Head :title="title || '{{ title }}'" />

    <div class="mb-6 space-y-1">
        <h1 class="text-2xl font-semibold tracking-tight">{{ title || '{{ title }}' }}</h1>
        <p class="text-sm text-muted-foreground">Built with the AdminPanel schema builder.</p>
    </div>

    <SchemaRenderer :schema="schema" :initial-data="initialData" />
</template>
